---
title: Island editor improvements — overview & plan index
type: overview
status: proposed
date: 2026-06-17
written_against_commit: b6dc287d
---

# Island editor improvements — overview & plan index

> Generated by the `improve` skill on 2026-06-17 at commit `b6dc287d`. Scope: the **standalone
> `island-editor/`** package (the "dedicated" editor — Vite + r3f/drei, its own isolated pnpm
> workspace, run with `pnpm --dir island-editor dev` → http://localhost:5180). Audit lenses requested:
> **editability, remaining works, quality, and agent-editability.**
>
> These plans live in `docs/plans/` (the repo's designated planning home — see the existing
> `2026-06-15-*` island initiative), **not** the `improve` skill's default root `plans/`.
>
> Each plan is self-contained and executable by an agent with zero context from the audit session.
> Execute in the order below; every plan carries a drift check (baseline `b6dc287d`) and STOP conditions.

## Verification baseline (at audit time)

`pnpm --dir island-editor typecheck` → exit 0. `pnpm --dir island-editor test` → **43 tests pass**
(5 files). The pure core (`islandSpec.ts`, `brush.ts`, `commandStack.ts`, validators) is clean,
framework-agnostic, and well-tested. These are the gates every plan cites.

## Architecture facts that frame the work

1. **The exported terrain spec is consumed by nothing.** `grep -rn "IslandSpec\|evaluateHeight" src/`
   (repo root) returns zero hits — the product engine (`src/engine/student-space/Game/State/Island.js`)
   still hard-codes terrain (`radius=5.0`, `silhouetteAt`, `heightAt`). The editor's seed even *copies*
   that silhouette (`islandSpec.ts:169`). This is **deferred-by-design** per the `2026-06-15-000`
   overview ("island shape/terrain editing (deferred v2)… collides with the ambient-visual rebuild").
2. **PR #74 made object placement & species data-driven but removed the in-app `#editor`.** The engine
   now boots from committed `defaultIslandLayout.json` (31 objects) + `defaultSpeciesPalette.json`; the
   standalone editor authors **terrain only**, not those layers.
3. **The standalone editor is an isolated pnpm workspace** — excluded from the repo's `pnpm check`
   gate (which is scoped to `src test`).

## Plan set & execution order

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|-----------|--------|
| 001 | Agent-editable island — **design spike** (op vocabulary, runner, binding) | P1 | M | LOW | none (stronger after 004) | DONE (branch `advisor/001-agent-island-editing-spike`, commit `ff660ef1`; doc `docs/island-editor-agent-editing-design.md` + PoC; unmerged) |
| 002 | Quality cleanups — unify spec validators + wire a check gate | P1 | S | LOW | none | DONE (branch `advisor/002-island-editor-quality-cleanups`, commit `9046efa3`; unmerged) |
| 003 | Fix per-frame full-field rebuild during coastline drag | P2 | M | MED | none | DONE (stacked on 004; branch `advisor/003-island-editor-coastline-drag-perf`, tip `0ed9058e`; unmerged; manual/browser checks pending) |
| 004 | Coastline add/insert/delete + numeric point & world-size entry | P2 | L | LOW | none | DONE (branch `advisor/004-island-editor-coastline-editing`, commit `33923726`; unmerged; manual/browser checks pending) |
| 005 | Brush cursor ring in sculpt mode | P3 | M | LOW | none | DONE (stacked on 003+004; branch `advisor/005-island-editor-brush-cursor`, tip `3a406279`; unmerged; manual/browser checks pending) |

Status values: TODO · IN PROGRESS · DONE · BLOCKED (one-line reason) · REJECTED (one-line rationale).

### Execution outcome (2026-06-17 — all 5 executed & reviewer-APPROVED, unmerged)

Dispatched via `improve execute` (cheaper executor subagents in isolated worktrees; advisor reviewed each diff). 003/004/005 share `App.tsx` so were **stacked** (004 → 003 → 005); 002 and 001 are file-disjoint and independent. Three branches to merge — **verified to octopus-merge with zero conflicts; combined `island-editor` suite green (typecheck 0, 52 tests)**:

```
git merge --no-edit advisor/002-island-editor-quality-cleanups   # 9046efa3 — validators + check gate
git merge --no-edit advisor/001-agent-island-editing-spike        # ff660ef1 — design doc + PoC
git merge --no-edit advisor/005-island-editor-brush-cursor        # 3a406279 — editor stack (004+003+005)
```

Order is immaterial (disjoint file sets). After merging, run `pnpm check:island-editor` + the browser checklist in each plan's Test plan (the visual criteria the headless executors could not verify).

### Code review + fixes (2026-06-17)

A `ce-code-review` pass (9 reviewers) on the integrated changeset found **no real P0/P1** — the two reported P1s were false positives (the coastline-drag effect's dep array + cleanup are pre-existing and unreachable with a single pointer; verified identical at `b6dc287d`). Four actionable findings were fixed on **`advisor/006-island-editor-review-fixes`** (`5893c35e`, stacked on the integration tip `5c75f494`):

- **#1** numeric inputs guarded via a `NumberField` (blocks `NaN`/`0`/`Infinity`, preserves negative-coordinate typing) + App-boundary `Number.isFinite` guards
- **#2** validate-before-autosave (`saveSpec` skips invalid specs → no silent reseed-to-default on reload)
- **#3** `movePoint` → `coastlineOps.movePointTo` dedup
- **#4** dead `selectedPoint` prop removed

Gates green (typecheck 0, **53 tests**). **`advisor/006` is now the single complete branch** (all 5 plans + review fixes) — merge/squash it instead of the three branches above. Full review + artifacts: `/tmp/compound-engineering/ce-code-review/20260617-104518-52930fb6/`.

### Recommended sequence

`002` (tiny, foundational — gives 001 a single canonical validator to reference) → `004` (its pure
`coastlineOps.ts` becomes the seed of 001's op vocabulary) → `003` (drag perf; matters more once 004
adds points) → `001` (the design spike, strongest once 004 exists to cite) → `005` (polish, anytime).

All five are independently executable — the ordering is leverage/coherence, not hard blockers.

## Dependency notes

- **001 (spike)** ships only a design doc; it has no code dependency, but reads best after **004** has
  created `coastlineOps.ts` (those helpers are literally the coastline op vocabulary the spike defines).
- **004** prescribes pure helpers (`insertPointAfter`/`deletePoint`/`movePointTo`) specifically so
  **001**'s future agent runner can import them rather than re-implement.
- **002** before **001**: the spike's "validation" section should point at `validateSpecObject` (the
  single validator 002 creates), not the two divergent ones that exist today.
- **003 + 004** are synergistic: 004 lets the coastline grow, which makes 003's drag-cost fix more
  valuable. Land 003 before or with 004 if doing both.

## Remaining works / direction (NOT built — maintainer's call)

These are real gaps the audit surfaced but did **not** turn into build plans (per the requester's
choices and honest scoping):

- **REMAIN-01 — Wire the terrain spec into the engine.** The editor's whole purpose (author terrain
  that ships) is unfulfilled today. Deferred-by-design (collides with the ambient-visual rebuild), and
  the requester chose a *design spike* over building. The cheapest first move is a consumption-contract
  investigation (how would `Island.js` read an `IslandSpec` heightfield, at what cost). **Effort L; partly
  blocked.** Plan 001's binding section documents that in-product agent editing is blocked on this.
- **REMAIN-02 — No GUI for placement/species.** Since #74, moving a tree means hand-editing
  `defaultIslandLayout.json`; species colors are hand-edited `defaultSpeciesPalette.json`. The
  standalone editor could grow a placement/palette mode (it already renders terrain in r3f). **Effort L.**
- **QUAL-05 — Relief is a dense 192² (36,864-float) grid** serialized whole on every autosave and
  export, almost always mostly-zero — and awkward for an agent to author/diff (tracked in 001's open
  questions). A sparse/RLE encoding or a lower default resolution would help once agent-editing is real.
  **Effort M; deferred** (changes the spec format → versioning).

## Findings considered and rejected (so they aren't re-audited)

- **QUAL-03 — Seed silhouette duplicated** between `islandSpec.ts:171` and `Island.js:31`. Real but
  isolated-by-design (keeps the editor free of the three@0.149 boundary); risk is silent drift if the
  engine silhouette is tuned. **Verdict: maintenance note, not a plan** — captured in 001/004 notes.
- **QUAL-06 — `INITIAL.relief` is module-level and mutated in place by the brush;** a StrictMode/HMR
  remount could reuse the mutated array as the ref seed. **Verdict: LOW confidence it manifests**
  (reset/import/reload all replace the ref; reload re-reads localStorage). Not worth a plan; revisit
  only if odd brush-state-on-reload is observed.
- **EDIT-04 — No orthographic/elevation camera presets** beyond "Top view". **Verdict: low value for a
  dev tool**; "Top view" plus orbit suffices.
- **AGENT live binding (in-product MCP/managed-agent tool)** — the requester explicitly chose
  "design spike only," and it's blocked on REMAIN-01 anyway. **Verdict: out of scope now**; revisit
  after the engine consumes a terrain spec.

## What was NOT audited

- The product engine (`src/engine/student-space/`) beyond confirming it does not consume `IslandSpec`.
- The `2026-06-15-*` in-engine island initiative (placement/species data model) — a separate concern;
  only its relationship to the standalone editor was mapped.
- Runtime/visual correctness of the r3f scene (shadows, lighting, materials) — out of the four
  requested lenses.
- Dependency CVE posture (`island-editor` has its own lockfile; not security-audited here).
